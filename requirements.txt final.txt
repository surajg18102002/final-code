GPT is working. Now just update the env variables and no code changes needed.

**Update these 3 env variables in Azure Function:**

| Name | Value |
|---|---|
| `GPT_ENDPOINT` | `https://aif-oai-dev-eastus2-01.cognitiveservices.azure.com/openai/deployments/rg-aif-dev-eastus2-01-model-router` |
| `GPT_MODEL` | `model-router` |
| `GPT_KEY` | `AirvBEEaEAMU4oBAje6nB774IWMYcHNZJlyyY5yruxBe4V5hSPn0JQQJ99BJACHYHv6XJ3w3AAAAACOGId4Q` |

Save → Confirm restart → test from Postman again.

The only remaining issue after this will be Tidal 401 which needs the new code with `TIDAL_AUTH_TOKEN` deployed first. So the order is:

1. Commit and deploy the new `function_app.py` to GitHub
2. Add `TIDAL_AUTH_TOKEN` env variable
3. Update `GPT_ENDPOINT` to the full deployment URL above
4. Test — both Tidal and GPT should work
