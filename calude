Looking at the code shared earlier, the endpoint is already there. Let me update the function with the correct GPT approach and env variable references.

The endpoint from your earlier code is:
```
https://aif-oai-dev-eastus2-01.cognitiveservices.azure.com
```

And the model-router pattern means the URL should be:
```
https://aif-oai-dev-eastus2-01.cognitiveservices.azure.com/models
```

**Step 1 — Add these to Azure Function Environment Variables:**

| Name | Value |
|---|---|
| `GPT_ENDPOINT` | `https://aif-oai-dev-eastus2-01.cognitiveservices.azure.com` |
| `GPT_KEY` | `AirvBEEaEAMU4oBAje6nB774IWMYcHNZJlyyY5yruxBe4V5hSPn0JQQJ99BJACHYHv6XJ3w3AAAAACOGId4Q` |
| `GPT_MODEL` | `model-router` |
| `TIDAL_API_KEY` | your Tidal x-api-key |

Remove `GPT_URL` if you already added it — it's replaced by `GPT_ENDPOINT` and `GPT_MODEL`.

**Step 2 — Update `requirements.txt`:****Paste both files into GitHub, then add these env variables in Azure Function:**

| Name | Value |
|---|---|
| `GPT_ENDPOINT` | `https://aif-oai-dev-eastus2-01.cognitiveservices.azure.com` |
| `GPT_KEY` | your GPT API key |
| `GPT_MODEL` | `model-router` |

**Remove `GPT_URL`** from env variables if you added it earlier — it's replaced by the three above.

The key change for GPT is it now uses the `azure-ai-inference` SDK with `ChatCompletionsClient` instead of a raw HTTP call, which is the correct way to call the model-router endpoint. Everything else stays the same.
