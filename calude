Now I have everything I need. The key differences from their working code:

1. **`Basic` capital B** (not `basic`)
2. **`User-Agent` header** they include
3. **`data={"data": xml_payload}`** as dict not raw string
4. **Username/password separately** base64 encoded, not pre-encoded token**Update these env variables in Azure Function — remove old ones and add these:**

| Name | Value |
|---|---|
| `TIDAL_BASE_URL` | `https://crt-apim.caresource.corp/TidalAutomation/` |
| `TIDAL_USER` | `caresource\CST_DashAuto_SVC` |
| `TIDAL_PASSWORD` | the actual password |
| `TIDAL_API_KEY` | `9d4e3372a8c7401b85a05be195e746e2` |

**Remove `TIDAL_AUTH`** — no longer needed since we now build it dynamically from username + password exactly like the working repo does.

The key fix is the `get_tidal_headers()` function which now does:
```python
credentials = f"{TIDAL_USERNAME}:{TIDAL_PASSWORD}"
auth_string = base64.b64encode(credentials.encode()).decode()
"Authorization": f"Basic {auth_string}"  # capital B
"User-Agent": "CareSource-TidalJobs/1.2"  # added
data={"data": xml_payload}               # dict not raw string
```
