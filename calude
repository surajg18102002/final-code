OK so the code is deployed. Then the env variables `TIDAL_USER` and `TIDAL_PASSWORD` are not set correctly. Let's verify.

**Go to Function App → Console and run:**
```
echo $TIDAL_USER
echo $TIDAL_PASSWORD
echo $TIDAL_BASE_URL
```

Tell me what it prints — if any of them are blank, that's the problem.

Also run this to see exactly what auth string is being built:
```python
python3 -c "
import base64, os
u = os.environ.get('TIDAL_USER', 'NOT SET')
p = os.environ.get('TIDAL_PASSWORD', 'NOT SET')
creds = f'{u}:{p}'
token = base64.b64encode(creds.encode()).decode()
print(f'User: {u}')
print(f'Pass: {p}')
print(f'Token: {token}')
print(f'Expected: Y2FyZXNvdXJjZVxDU1RfRGFzaEF1dG9fU1ZDOjUyM2VSUF46aHhRuEk3R2tFVmd6aTFOTl42dXZpdw==')
print(f'Match: {token == \"Y2FyZXNvdXJjZVxDU1RfRGFzaEF1dG9fU1ZDOjUyM2VSUF46aHhRuEk3R2tFVmd6aTFOTl42dXZpdw==\"}')
"
```

This will tell us immediately if the token being built matches the working curl token. If `Match: False` we know exactly what's wrong with the credentials.
